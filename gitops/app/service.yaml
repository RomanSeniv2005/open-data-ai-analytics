apiVersion: v1
kind: Service
metadata:
  name: open-data-web-service
  namespace: open-data-app
spec:
  type: NodePort
  selector:
    app: open-data-web
  ports:
    - name: http
      protocol: TCP
      port: 8000
      targetPort: 8000
      nodePort: 30080